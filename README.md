# 🔥 Ultimate Firewall Bypass Scanner

[![Python](https://img.shields.io/badge/Python-3.8+-blue.svg)](https://www.python.org/)
[![License](https://img.shields.io/badge/License-MIT-green.svg)](LICENSE)
[![Requirements](https://img.shields.io/badge/Status-Active-success.svg)](https://github.com/fylhtq03/Firewall-Bypass-Scanner)

**Полностью автоматический сканер портов для пентеста**, который самостоятельно перебирает десятки методов обхода фаерволов и DPI систем. Больше не нужно вручную подбирать параметры для `nmap` — этот сканер сделает всё сам.

---

## 📌 Что делает этот сканер?

Современные корпоративные фаерволы (Palo Alto, Fortinet, Check Point) с технологиями **Deep Packet Inspection (DPI)** легко обнаруживают и блокируют стандартные сканирования. Этот инструмент решает проблему, автоматически применяя все известные техники обхода:

- ✅ **Автоматический перебор** всех методов сканирования (SYN, FIN, NULL, XMAS, ACK и др.)
- ✅ **Интеллектуальный анализ** типа фаервола и подбор оптимальной стратегии
- ✅ **Полная автономность** — не требует nmap/masscan
- ✅ **Детальная отчетность** в JSON и текстовом формате

---

## 🚀 Основные возможности

| Функция | Описание |
|---------|----------|
| **Автоматический обход DPI** | Фрагментация пакетов, изменение TTL, маскировка под легитимный трафик |
| **Полный набор методов** | 13+ TCP методов + UDP сканирование с различными параметрами |
| **Адаптивное сканирование** | Автоматически определяет тип фаервола и выбирает стратегию |
| **Детекция сервисов** | Определение баннеров и версий ПО |
| **Многопоточность** | Параллельное сканирование портов для максимальной скорости |

---

## 📦 Установка и настройка

### Требования
- Python 3.8 или выше
- **Обязательно: права root** (для работы с raw sockets)
- Linux (рекомендуется Ubuntu/Debian)

### Установка
```bash
# Клонируем репозиторий
git clone https://github.com/fylhtq03/Firewall-Bypass-Scanner.git
cd Firewall-Bypass-Scanner

# Устанавливаем зависимости
sudo apt-get update
sudo apt-get install python3 python3-pip
pip3 install --upgrade pip
pip3 install -r requirements.txt
# Проверяем права
sudo python3 ultimate_scanner.py --help
```
🎯 Примеры использования
Быстрая разведка (рекомендуется сначала)
```bash
sudo python3 ultimate_scanner.py 192.168.1.1 --top-ports 100
```
*Сканирует 100 самых популярных портов за 10-30 минут*
Обход корпоративного фаервола
```bash
sudo python3 ultimate_scanner.py target-corp.com --range 1 5000 --tests-per-port 50
```
Использует максимум техник для обхода строгой защиты
Полное сканирование (очень долго!)
```bash
sudo python3 ultimate_scanner.py target.com --full --tests-per-port 20
```
*⚠️ ВНИМАНИЕ: 1-65535 портов могут сканироваться несколько дней*
📊 Что сканер автоматически перебирает?
Методы сканирования:
SYN — стандартное стелс-сканирование

FIN — обход stateful фаерволов

NULL — пакеты без флагов

XMAS — FIN+URG+PSH (рождественская ёлка)

ACK — определение правил фаервола

MAIMON, WINDOW, URG, PSH, RST и другие

Параметры (каждый метод тестируется с разными):
| Функция | Описание |
|---------|----------|
| **Манипуляция TTL** | Изменение Time-To-Live (1, 16, 32, 64, 128, 255) для обхода фильтрации по расстоянию до хоста |
| **Вариация размеров окна** | Использование разных размеров TCP-окна (0-65535, 12 значений) для имитации различных операционных систем |
| **Подмена исходного порта** | Использование доверенных портов (53, 80, 443, 22, 25 и др.) для маскировки под легитимный сетевой трафик |
| **Фрагментация MTU** | Изменение максимального размера пакета (8, 16, 24, ..., 1500 байт) для обхода систем глубокого анализа (DPI) |
| **Кастомизация TCP-опций** | Применение 5 различных наборов TCP-опций для точной имитации стеков разных операционных систем |



*⚠️ Легальность*
ВСЕГДА получайте письменное разрешение перед сканированием
Используйте только на своих системах или системах с разрешением
Сохраняйте логи всех действий для отчетности

*⚠️ Сетевое воздействие*
Сканирование генерирует большой объем трафика
Может быть легко обнаружено IDS/IPS системами
Не используйте в производственных сетях без уведомления

*⚠️ Производительность*
Для локальных сетей: --tests-per-port 100 --max-workers 50
Для интернет-сканирования: --tests-per-port 30 --max-workers 20
Для скрытности: --stealth --tests-per-port 10 --timeout 5
